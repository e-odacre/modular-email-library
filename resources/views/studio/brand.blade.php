@extends('studio.layout')
@section('title', $collection['name'])
@section('content')
<div class="flex flex-wrap items-center justify-between gap-4"><div><a class="link link-hover text-xs text-base-content/50" href="{{ route('studio.library', 'brands') }}">← Brands</a><div class="mt-2 flex items-center gap-3"><h1 class="text-2xl font-semibold">{{ $collection['name'] }}</h1><span class="badge badge-warning badge-outline badge-sm">Draft collection</span></div><p class="mt-2 text-sm text-base-content/60">{{ count($collection['blocks']) }} blocks · {{ count($collection['emails']) }} emails · {{ count($collection['themes']) }} styles</p></div><a class="btn btn-primary btn-sm" href="{{ route('workflow.create', ['brand' => $collection['brand'], 'theme' => $theme]) }}">+ New email for this brand</a></div>
<details class="rounded-lg border border-warning/30 bg-warning/5 p-4"><summary class="text-sm font-medium">Brand values need review <span class="ml-2 text-xs text-base-content/50">Draft notes</span></summary><ul class="mt-3 list-inside list-disc space-y-1 text-xs text-base-content/70">@foreach(array_merge($collection['notes'] ?? [], $collection['placeholderIssues']) as $note)<li>{{ $note }}</li>@endforeach</ul></details>
<div class="tabs tabs-border" role="tablist" aria-label="Collection type">@foreach(['emails' => 'Complete emails', 'blocks' => 'Blocks'] as $value => $label)<a role="tab" aria-selected="{{ $group === $value ? 'true' : 'false' }}" class="tab {{ $group === $value ? 'tab-active' : '' }}" href="{{ route('studio.collection', ['brand' => $collection['brand'], 'kind' => $value, 'theme' => $theme]) }}">{{ $label }} <span class="badge badge-ghost badge-xs ml-2">{{ count($collection[$value]) }}</span></a>@endforeach</div>
@include('studio.library')
@endsection
