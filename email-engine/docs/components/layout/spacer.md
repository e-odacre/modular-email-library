<!-- Generated by `npm run docs` from the component metadata. Do not edit by hand, edit the .meta.js file instead. -->
# Spacer

`layout/spacer` &middot; content-level &middot; category: layout

## Purpose

Vertical space between content components inside a column.

## Required fields

None.

## Optional fields

None.

## Variants

Same data and structure, different styling. Pick one with `{ variant: 'name' }`.

- **default**: Medium space (24px).
- **small**: Small space (8px). Sets `height: @spacing.sm`.
- **large**: Large space (48px). Sets `height: @spacing.2xl`.

## Settings

Every setting has a default. Values may be token references (`@colors.accent`, `@theme.card.radius`). Pass overrides with `{ settings: { ... } }`.

| Setting | Type | Default |
| --- | --- | --- |
| `hideOnMobile` | boolean | `false` |
| `hideOnDesktop` | boolean | `false` |
| `height` | length | `"@spacing.lg"` |

## Responsive behavior

- `hideOnMobile`: hides it below 480px. Outlook desktop and Gmail on non-Google accounts ignore media queries and still show it.
- `hideOnDesktop`: shows it only on phones. Clients that ignore media queries keep it hidden.

## Compatibility

| Outlook desktop | Gmail apps | Dark mode |
| --- | --- | --- |
| Fixed-height table cell, honored. | Fine. | Empty space, nothing to swap. |



## Example usage

This is a content-level component: it goes inside a column, so put it in the `content` of a `layout/section` (or another layout).

In an email (`.mjml`):

```
[[ c('layout/spacer', {}) ]]
```

As data (a node, for recipes and the remix API):

```json
{
  "component": "layout/spacer",
  "data": {}
}
```
