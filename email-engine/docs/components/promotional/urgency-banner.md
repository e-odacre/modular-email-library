<!-- Generated by `npm run docs` from the component metadata. Do not edit by hand, edit the .meta.js file instead. -->
# Urgency Banner

`promotional/urgency-banner` &middot; section-level &middot; category: promotional

## Purpose

A band that says time is running out: the deadline, a headline, a line of text and a button. Use only for real deadlines.

## Required fields

| Field | Type | Default |
| --- | --- | --- |
| `deadline` | text |  |
| `headline` | text |  |

## Optional fields

| Field | Type | Default |
| --- | --- | --- |
| `text` | text |  |
| `cta` | cta { label, href } |  |

## Variants

Same data and structure, different styling. Pick one with `{ variant: 'name' }`.

- **default**: White text on the error (red) color.
- **dark**: White text on the primary brand color. Sets `background: @colors.primary`, `buttonColor: @colors.primary`.
- **accent**: White text on the accent color. Sets `background: @colors.accent`, `buttonColor: @colors.accent`.
- **light**: Dark text on the surface color. Sets `background: @colors.surface`, `textColor: @colors.text`, `buttonBackground: @colors.primary`, `buttonColor: @colors.primaryText`.

## Settings

Every setting has a default. Values may be token references (`@colors.accent`, `@theme.card.radius`). Pass overrides with `{ settings: { ... } }`.

| Setting | Type | Default |
| --- | --- | --- |
| `hideOnMobile` | boolean | `false` |
| `hideOnDesktop` | boolean | `false` |
| `align` | alignment | `"center"` |
| `background` | color | `"@colors.error"` |
| `textColor` | text | `"@colors.white"` |
| `buttonBackground` | color | `"@colors.white"` |
| `buttonColor` | color | `"@colors.error"` |
| `padding` | spacing | `"@spacing.xl @theme.section.paddingX"` |

## Responsive behavior

- `hideOnMobile`: hides it below 480px. Outlook desktop and Gmail on non-Google accounts ignore media queries and still show it.
- `hideOnDesktop`: shows it only on phones. Clients that ignore media queries keep it hidden.

## Compatibility

| Outlook desktop | Gmail apps | Dark mode |
| --- | --- | --- |
| Tables and text honored. | Fine. | Light variant swaps with the surface. Colored bands keep their colors. |



## Example usage

This is a section-level component: use it directly in an email body or a recipe.

In an email (`.mjml`):

```
[[ c('promotional/urgency-banner', {
  "deadline": "Ends tonight at midnight",
  "headline": "Your 25% off is about to expire",
  "text": "The summer edit goes back to full price tomorrow.",
  "cta": {
    "label": "Use my discount",
    "href": "https://example.com/collections/summer"
  }
}) ]]
```

As data (a node, for recipes and the remix API):

```json
{
  "component": "promotional/urgency-banner",
  "data": {
    "deadline": "Ends tonight at midnight",
    "headline": "Your 25% off is about to expire",
    "text": "The summer edit goes back to full price tomorrow.",
    "cta": {
      "label": "Use my discount",
      "href": "https://example.com/collections/summer"
    }
  }
}
```
