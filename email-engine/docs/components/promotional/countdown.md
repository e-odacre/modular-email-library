<!-- Generated by `npm run docs` from the component metadata. Do not edit by hand, edit the .meta.js file instead. -->
# Countdown

`promotional/countdown` &middot; section-level &middot; category: promotional

## Purpose

A countdown block: a timer image from an external service, with the end time as text. A static approximation, not a live timer.

## Required fields

| Field | Type | Default |
| --- | --- | --- |
| `endsAt` | text |  |

## Optional fields

| Field | Type | Default |
| --- | --- | --- |
| `headline` | text |  |
| `timerImage` | url |  |
| `cta` | cta { label, href } |  |

## Variants

Same data and structure, different styling. Pick one with `{ variant: 'name' }`.

- **default**: White text on the primary color.
- **dark**: White text on the primary brand color. Sets `background: @colors.primary`, `buttonColor: @colors.primary`.
- **urgent**: White text on the error (red) color, for real urgency. Sets `background: @colors.error`, `buttonColor: @colors.error`.
- **light**: Dark text on the surface color, with a solid button. Sets `background: @colors.surface`, `textColor: @colors.text`, `buttonBackground: @colors.primary`, `buttonColor: @colors.primaryText`.
- **accent**: White text on the accent color. Sets `background: @colors.accent`, `buttonColor: @colors.accent`.

## Settings

Every setting has a default. Values may be token references (`@colors.accent`, `@theme.card.radius`). Pass overrides with `{ settings: { ... } }`.

| Setting | Type | Default |
| --- | --- | --- |
| `hideOnMobile` | boolean | `false` |
| `hideOnDesktop` | boolean | `false` |
| `align` | alignment | `"center"` |
| `background` | color | `"@colors.primary"` |
| `textColor` | text | `"@colors.white"` |
| `buttonBackground` | color | `"@colors.white"` |
| `buttonColor` | color | `"@colors.primary"` |
| `padding` | spacing | `"@spacing.xl @theme.section.paddingX"` |
| `label` | text | `"Offer ends"` |

## Responsive behavior

- `hideOnMobile`: hides it below 480px. Outlook desktop and Gmail on non-Google accounts ignore media queries and still show it.
- `hideOnDesktop`: shows it only on phones. Clients that ignore media queries keep it hidden.

## Compatibility

| Outlook desktop | Gmail apps | Dark mode |
| --- | --- | --- |
| A normal image. Animated timers show only the first frame. | Images go through a proxy that can cache them, so a timer can show a stale time. | Light variant swaps with the surface. Colored bands keep their colors. |



## Example usage

This is a section-level component: use it directly in an email body or a recipe.

In an email (`.mjml`):

```
[[ c('promotional/countdown', {
  "headline": "The sale ends soon",
  "endsAt": "Sunday 30 June at midnight PT",
  "cta": {
    "label": "Shop before it ends",
    "href": "https://example.com/collections/sale"
  }
}) ]]
```

As data (a node, for recipes and the remix API):

```json
{
  "component": "promotional/countdown",
  "data": {
    "headline": "The sale ends soon",
    "endsAt": "Sunday 30 June at midnight PT",
    "cta": {
      "label": "Shop before it ends",
      "href": "https://example.com/collections/sale"
    }
  }
}
```
