<!-- Generated by `npm run docs` from the component metadata. Do not edit by hand, edit the .meta.js file instead. -->
# Outlook-safe Button

`email/outlook-safe-button` &middot; content-level &middot; category: email

## Purpose

A fixed-size call-to-action button with true rounded corners in Outlook desktop, drawn with VML.

## Required fields

| Field | Type | Default |
| --- | --- | --- |
| `label` | text |  |
| `href` | url |  |

## Optional fields

None.

## Variants

Same data and structure, different styling. Pick one with `{ variant: 'name' }`.

- **default**: Solid button, 220 by 48.
- **wide**: A wider button (300 by 52). Sets `width: 300px`, `height: 52px`.
- **pill**: Fully rounded ends. Sets `radius: @radius.pill`.

## Settings

Every setting has a default. Values may be token references (`@colors.accent`, `@theme.card.radius`). Pass overrides with `{ settings: { ... } }`.

| Setting | Type | Default |
| --- | --- | --- |
| `hideOnMobile` | boolean | `false` |
| `hideOnDesktop` | boolean | `false` |
| `width` | length | `"220px"` |
| `height` | length | `"48px"` |
| `radius` | length | `"@theme.button.radius"` |
| `background` | color | `"@colors.primary"` |
| `color` | color | `"@colors.primaryText"` |
| `align` | alignment | `"center"` |
| `padding` | spacing | `"@spacing.sm 0"` |

## Responsive behavior

- `hideOnMobile`: hides it below 480px. Outlook desktop and Gmail on non-Google accounts ignore media queries and still show it.
- `hideOnDesktop`: shows it only on phones. Clients that ignore media queries keep it hidden.

## Compatibility

| Outlook desktop | Gmail apps | Dark mode |
| --- | --- | --- |
| True rounded corners via VML roundrect, whole shape clickable. | A normal inline-block link with border-radius. | Keeps its own colors. |



## Example usage

This is a content-level component: it goes inside a column, so put it in the `content` of a `layout/section` (or another layout).

In an email (`.mjml`):

```
[[ c('email/outlook-safe-button', {
  "label": "Shop the sale",
  "href": "https://example.com/collections/sale"
}) ]]
```

As data (a node, for recipes and the remix API):

```json
{
  "component": "email/outlook-safe-button",
  "data": {
    "label": "Shop the sale",
    "href": "https://example.com/collections/sale"
  }
}
```
