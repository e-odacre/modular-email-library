<!-- Generated by `npm run docs` from the component metadata. Do not edit by hand, edit the .meta.js file instead. -->
# Text

`content/text` &middot; content-level &middot; category: content

## Purpose

A paragraph of body copy in one of the body type levels.

## Required fields

| Field | Type | Default |
| --- | --- | --- |
| `text` | text |  |

## Optional fields

None.

## Variants

Same data and structure, different styling. Pick one with `{ variant: 'name' }`.

- **default**: Standard body copy.
- **large**: Larger lead paragraph. Sets `level: bodyLarge`.
- **small**: Smaller supporting text. Sets `level: bodySmall`.
- **caption**: Caption or fine print in the muted color. Sets `level: caption`.
- **centered**: Centered body copy. Sets `align: center`.

## Settings

Every setting has a default. Values may be token references (`@colors.accent`, `@theme.card.radius`). Pass overrides with `{ settings: { ... } }`.

| Setting | Type | Default |
| --- | --- | --- |
| `mobileAlignment` | one of `inherit`, `left`, `center`, `right` | `"inherit"` |
| `hideOnMobile` | boolean | `false` |
| `hideOnDesktop` | boolean | `false` |
| `level` | one of `body`, `bodyLarge`, `bodySmall`, `caption` | `"body"` |
| `align` | alignment | `"left"` |
| `color` | text | `"auto"` |
| `padding` | spacing | `"0 0 @spacing.md 0"` |
| `darkMode` | boolean | `true` |

## Responsive behavior

- `mobileAlignment`: `inherit`, `left`, `center`, `right`. Overrides text alignment below 480px (`inherit` keeps the desktop alignment). Best effort, images depend on the client.
- `hideOnMobile`: hides it below 480px. Outlook desktop and Gmail on non-Google accounts ignore media queries and still show it.
- `hideOnDesktop`: shows it only on phones. Clients that ignore media queries keep it hidden.

## Compatibility

| Outlook desktop | Gmail apps | Dark mode |
| --- | --- | --- |
| Line height and padding honored. | Fine. | Text color swaps via dm-text where honored. Set darkMode off on colored backgrounds. |



## Example usage

This is a content-level component: it goes inside a column, so put it in the `content` of a `layout/section` (or another layout).

In an email (`.mjml`):

```
[[ c('content/text', {
  "text": "Made for slower mornings and warmer days. Our summer collection is cut from breathable linen and washed cotton."
}) ]]
```

As data (a node, for recipes and the remix API):

```json
{
  "component": "content/text",
  "data": {
    "text": "Made for slower mornings and warmer days. Our summer collection is cut from breathable linen and washed cotton."
  }
}
```
