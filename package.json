{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build",
        "dev": "vite",
        "email:install": "npm ci --prefix email-engine",
        "email:test": "npm test --prefix email-engine",
        "email:build": "npm run build --prefix email-engine",
        "email:themes": "npm run build:themes --prefix email-engine",
        "email:gallery": "npm run gallery:brand --prefix email-engine -- sanecotec"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.0.0",
        "concurrently": "^10.0.3",
        "laravel-vite-plugin": "^3.1",
        "tailwindcss": "^4.0.0",
        "vite": "^8.0.0"
    },
    "optionalDependencies": {
        "@laravel/multiplex": "^0.4.1"
    }
}
